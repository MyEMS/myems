'use strict';

/**
 * Common Constants
 * 
 * 统一管理所有 Tab 索引常量，避免在多个控制器中硬编码
 */

// Tenant Tab Indexes
var TENANT_TAB_INDEXES = {
    TENANT: 0,
    TENANT_TYPE: 1,
    BIND_METER: 2,
    BIND_POINT: 3,
    BIND_SENSOR: 4,
    BIND_WORKING_CALENDAR: 5,
    BIND_COMMAND: 6
};

// Store Tab Indexes
var STORE_TAB_INDEXES = {
    STORE: 0,
    BIND_METER: 1,
    BIND_POINT: 2,
    BIND_SENSOR: 3,
    BIND_WORKING_CALENDAR: 4,
    BIND_COMMAND: 5
};

// Shopfloor Tab Indexes
var SHOPFLOOR_TAB_INDEXES = {
    SHOPFLOOR: 0,
    BIND_METER: 1,
    BIND_EQUIPMENT: 2,
    BIND_POINT: 3,
    BIND_SENSOR: 4,
    BIND_WORKING_CALENDAR: 5,
    BIND_COMMAND: 6
};

// Equipment Tab Indexes
var EQUIPMENT_TAB_INDEXES = {
    EQUIPMENT: 0,
    BIND_METER: 1,
    BIND_DATA_SOURCE: 2,
    BIND_PARAMETER: 3,
    BIND_COMMAND: 4
};

// Combined Equipment Tab Indexes
var COMBINED_EQUIPMENT_TAB_INDEXES = {
    COMBINED_EQUIPMENT: 0,
    BIND_EQUIPMENT: 1,
    BIND_METER: 2,
    BIND_DATA_SOURCE: 3,
    BIND_PARAMETER: 4,
    BIND_COMMAND: 5
};

// Meter Tab Indexes
var METER_TAB_INDEXES = {
    METER: 0,
    BIND_POINT: 1,
    TREE_VIEW: 2,
    VIRTUAL_METER: 3,
    OFFLINE_METER: 4,
    OFFLINE_METER_FILE: 5,
    BIND_COMMAND: 6
};

// Space Tab Indexes
var SPACE_TAB_INDEXES = {
    SPACE: 0,
    METER: 1,
    EQUIPMENT: 2,
    COMBINED_EQUIPMENT: 3,
    POINT: 4,
    SENSOR: 5,
    TENANT: 6,
    STORE: 7,
    SHOPFLOOR: 8,
    WORKING_CALENDAR: 9,
    COMMAND: 10,
    ENERGY_STORAGE_POWER_STATION: 11,
    ENERGY_FLOW_DIAGRAM: 12,
    DISTRIBUTION_SYSTEM: 13,
    PHOTOVOLTAIC_POWER_STATION: 14,
    MICROGRID: 15
};

// 导出所有 Tab 索引常量（供全局使用）
app.constant('TAB_INDEXES', {
    TENANT: TENANT_TAB_INDEXES,
    STORE: STORE_TAB_INDEXES,
    SHOPFLOOR: SHOPFLOOR_TAB_INDEXES,
    EQUIPMENT: EQUIPMENT_TAB_INDEXES,
    COMBINED_EQUIPMENT: COMBINED_EQUIPMENT_TAB_INDEXES,
    METER: METER_TAB_INDEXES,
    SPACE: SPACE_TAB_INDEXES
});

